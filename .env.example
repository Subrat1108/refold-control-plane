# .env.example — commit this file, never commit .env
# All data is currently mocked; these are for when the real backend is wired up.

# Base URL for the Refold API
VITE_API_BASE_URL=https://api.refold.io

# Which environment this deployment targets (development | staging | production)
VITE_ENV=production
